feedparser==6.0.11
requests==2.32.3
# Optional — only needed if you use --ai. Safe to leave installed; the script
# imports it lazily and falls back to the heuristic synthesis if it's missing.
anthropic==0.39.0
